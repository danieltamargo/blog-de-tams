export interface IAnimNew {
    name: string;
    duration: string;
    easing: string;
}
  
export interface IAnimOld {
    name: string;
    duration: string;
    easing: string;
    direction: string;
}